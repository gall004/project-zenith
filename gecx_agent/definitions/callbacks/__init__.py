"""CES callback definitions.

Python source files in this directory contain callback functions that are
registered on CES agents via the provisioning pipeline. They run
server-side in the CES Python sandbox at deterministic lifecycle points.
"""
